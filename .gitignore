web/node_modules/
web/dist/
web/.env.local
# Empaquetado de Chrome: el .pem es llave privada (jamás al repo), el .crx no sirve para compartir
*.pem
*.crx
# Notas internas: solo locales, no van al repo
investigacion.md
plan-backend.md
